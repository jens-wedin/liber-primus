__pycache__/
model_cache/
keytext_cache/
.kjv_src/
.DS_Store
data/pages/

# N10 resume cache (scratch, reproducible)
results/gromark_ckpt*.json

# N19: Dukotah vision transcription (external, 1.7MB — fetch on demand)
download/dukotah_read4_vision.json

# N1: relikd LiberPrayground InterruptDB (external, ~1.7MB — fetch on demand)
download/relikd_interruptdb/
.pytest_cache/

# LaTeX build artifacts (the paper source is committed; the PDF is generated)
paper/*.pdf
paper/*.aux
paper/*.log
paper/*.out

# HistoCrypt author-kit archive (the extracted .sty/.bst are committed under paper/)
docs/histocrypt.zip
