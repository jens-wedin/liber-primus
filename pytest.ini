[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: control/integration tests that run the beam pipelines (seconds each)
# Default run is the fast invariant suite; add `-m slow` (or `-m ""`) for the
# plant-and-recover control tests. Run BOTH before committing.
addopts = -m "not slow"
